apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: atlan-middleware
  namespace: atlas

resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml

commonLabels:
  app: atlan-middleware
  version: v1.0.0

images:
  - name: ghcr.io/sumandas0/atlan-requests-test
    newTag: main-3870e27